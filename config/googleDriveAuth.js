// const fetchObj = require('isomorphic-fetch');

class GoogleDriveAuth {
  constructor() {
    console.log('Initializing GoogleDriveAuth');
    // const appAccessToken = process.env.DROPBOX_ACCESS_TOKEN;
    // if (!dbxInstance) {
    //   // dbxInstance = dbx1.Dropbox({ accessToken: this.appAccessToken });
    //   infoLoggerInstance.info(`dbxInstance Access Token ${appAccessToken}`);
    //   dbxInstance = new Dropbox({ accessToken: appAccessToken, fetch: fetchObj });
    // }
    // return dbxInstance;
  }
}

module.exports = GoogleDriveAuth;
